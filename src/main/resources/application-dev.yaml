upload:
  base-path: /Volumes/Download/mist-cloud
  checkmd5: false
front:
  host: localhost
  port: 8000

server:
  port: 8888

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mist_cloud_dev?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: root

